# NS P2 — Current Attack Map (2026-09-12)

**Status:** live research map; not a proof claim  
**Canonical architecture:** `paper/NS_P2_STANDALONE_VNEXT.md`  
**Claim boundary:** G4 / G6 / G7 / Clay regularity remain OPEN  
**Rule:** reuse-first; finite-first; no continuum shortcut; no type drift between distinct holonomy objects.

---

## 0. End-to-end map

```text
finite registered NSE events / readers
        |
        v
G4  exact compact scale-covariant quotient                         OPEN
        |
        v
G6  zero-defect chain-recurrent rigidity                           OPEN  <-- primary hard theorem
        |
        v
G7  quantitative exit / defect gap                                 OPEN
        |
        v
V_{j+m} <= (1-delta) V_j + epsilon_j
        |
        v
R_j -> 0 / S_j -> 0
        |
        v
NS-P2-FINAL-EQUIV(B)                                               DERIVED adapter step
        |
        v
regularity under declared final adapter
```

Nothing below changes the Clay status: **OPEN**.

---

## 1. G6 interior map — current split

Canonical recurrent target:

\[
\operatorname{CR}_0(\overline{\mathcal Q}^{sc}_{P2})
\subseteq \mathcal P\cup\mathcal D.
\]

The current interior route is no longer organised by one generic notion of `holonomy`. It splits typed objects.

### Type P — projective polarization / lineage holonomy

Registered null/projective transport:

\[
T_e:\mathbb P(k_1^\perp)\to\mathbb P(k_2^\perp),
\qquad
T_{\rm loop}(\lambda)=\lambda.
\]

Plane-switch reconstruction already gives, on a genuine switch,

\[
[c_{j+1}]=[n_j\times n_{j+1}].
\]

Current finite rigidity proposal:

\[
\boxed{
M\in PGL(2,\mathbb R),\quad
M\ell_i=\ell_i\ (i=1,2,3),\quad
\ell_1,\ell_2,\ell_3\ \text{distinct}
\Rightarrow M=I.
}
\]

Status: exact finite algebra checked; repository proposal
`PROP-P3-PROJECTIVE-THREE-LINE-RIGIDITY-01`.

This creates the current Type-P fork:

```text
productive recurrent projective web
        |
        +--> M != I
        |      |
        |      +--> at most two fixed projective lineage classes
        |      +--> THIRD-LINE GENERATION / RETURN theorem          OPEN
        |
        +--> M = I
               |
               +--> productive identity-monodromy rigidity          OPEN
               +--> target: planar / degenerate / terminal / exit / defect
```

### Immediate Type-P target A — nonidentity branch

Prove or refute:

\[
\boxed{
M\neq I
+\text{productive full-convolution recurrence}
\Rightarrow
\text{third future-relevant returned projective lineage}
\ \vee\
\text{registered terminal/exit/defect/resolution}.
}
\]

Falsification first: search for an exact productive recurrent full-convolution web whose returned future-relevant lineage classes remain on only two projective lines.

Status: **OPEN**.

### Immediate Type-P target B — identity branch

Prove or refute:

\[
\boxed{
M=I
+\text{productivity}
+D=E=0
\Rightarrow
\text{planar / degenerate / terminal}.
}
\]

Status: **OPEN**. This is the hard equality branch.

---

## 2. Mandatory type boundary: Type P is not Type Phi

### Type Phi — complex scalar phase-incidence holonomy

This is the existing Fourier scalar-channel phase object. For a declared conflict cycle with `pi` target mismatch, the existing finite packing theorem gives

\[
\sum_eT_e\le\sum_eA_e-c_0P,
\qquad
c_0=1-\frac1{\sqrt2}.
\]

Window integration is also already derived, so arbitrary switching of which edge pays the phase tax does not erase the packed tax.

### Forbidden inference

The following is **not** proved and must not be used:

```text
nontrivial Type-P projective holonomy
    -> Type-Phi pi phase cycle
    -> packed phase tax.
```

The missing bridge is:

\[
\boxed{
\text{Type-P recurrent lineage/plane-switch web}
\Rightarrow
\text{actual registered full-convolution channel network}
\Rightarrow
\begin{cases}
\text{Type-Phi conflict-cycle coverage},\\
\text{or classified conflict-poor remainder}.
\end{cases}
}
\]

Status: **OPEN**.

---

## 3. Reused Type-Phi / conflict-rich branch

Already available and reusable after the type bridge:

```text
exact local pi-holonomy tax                                  DERIVED
non-overcounted fractional cycle packing                     DERIVED
packing -> strict relative tax                               DERIVED
small packing -> small weighted hitting set                  DERIVED
window-integrated packed tax                                 DERIVED
fast phase switching evades packed tax                       REFUTED
```

Do not rebuild these theorems. The open issue is coverage/realization from the actual full-convolution event web.

---

## 4. Reused conflict-poor / fresh-donor branch

Exact negative control:

```text
cycle-free / phase-SAT selected fresh-donor chain
    can still be dyadically progressive and H1-nondegenerate.
```

Therefore

```text
no Type-Phi cycle -> weak transfer
```

is **REFUTED**.

But the selected fresh-donor tree is not dynamically closed. Consecutive donors generate an off-tree target with cutoff-uniform H1 strength. Existing window charge:

\[
\frac1{\sqrt2}
\int_I|X_{q_0}X_{q_1}|\,dt
\le
\int_I|D_r|\,dt
+
\int_I|R_r|\,dt.
\]

Large high-scale donors also have recent nonlinear ancestry rather than being supplied freely by positive-lag linear memory.

Current conflict-poor target:

\[
\boxed{
\text{recursive recent-nonlinear ancestry / cancellation closure}
\Rightarrow
\text{window loss or registered defect/exit}.
}
\]

Status: **OPEN**.

Important red-team already retained:

```text
simple mirror cancellation exists                            DERIVED
cancellation automatically costly                            REFUTED
```

So the proof must recursively account for cancellation; merely naming it `complexity` is invalid.

---

## 5. OCSR / Witness-Soundness route

Standalone vNext preferred interior move remains:

```text
OCSR
+ global multi-source cancellation compatibility
+ Witness Soundness
    -> interior recurrent rigidity candidate
```

Current exact material:

```text
W1/W2/W3 exact cancellation fixtures                         PASS
W3 generation-1 outward suppression                         PASS
W3 depth-1 full convolution not lossless                     PASS
rank-2 polarization-plane rigidity proposals                 exact finite PASS / proposal
plane-switch capacity / lineage reconstruction               exact finite PASS / proposal
```

Still OPEN:

```text
Witness Soundness for D_cancel
OCSR globally
FNW globally
global multi-source cancellation compatibility
```

Critical rule:

\[
C_k>0\not\Rightarrow\text{physical/payable defect}.
\]

Cancellation amount and accounting defect remain distinct.

---

## 6. Boundary map

### Lower boundary / FAR

Weighted ancestry reader:

\[
\mathcal A_{N,\eta}\asymp
\sup_{L\le N}
\left(\frac LN\right)^{2\eta}S_L.
\]

FAR estimate:

\[
G_M\lesssim
\mathcal A_{N,\eta}^{1/2}
2^{-M(\beta-\eta)}.
\]

Analytic suppression is strongly derived at sketch level, but the required uniform ancestry control on one coherent genealogy remains **OPEN**.

### Upper boundary / diffuse remote-high residue

Per-triad remote-high null suppression is exact, but aggregate packet closure is not automatic. Boundary FixRefine remains required:

```text
identifiability -> dynamic sufficiency -> zero-defect viability
```

Upper-boundary behavior quotient remains **OPEN**.

---

## 7. G4 / G7 map

### G4 — exact compact scale-covariant quotient

Need one normalized compact live path space preserving registered readers, lineage, event routing and boundary fibers.

Status: **OPEN**.

### G7 — quantitative exit from exact-zero rigidity

Preferred route:

1. prove exact zero-defect productive recurrence impossible on the normalized live locus;
2. use compactness + lower semicontinuity to obtain

\[
\delta_*=\min_{K_{live}}D_{tot}>0;
\]

3. use covariance/homogeneity to get

\[
D_{tot}\ge\delta_*\mathcal V;
\]

4. conclude a recurrence

\[
\mathcal V_{j+m}\le(1-\delta_*)\mathcal V_j+\varepsilon_j.
\]

Status: **OPEN**. Do not attempt universal contraction directly from energy; that route has already been refuted/audited.

---

## 8. Current attack order

```text
A1. NONIDENTITY TYPE-P FALSIFICATION
    Can productive full-convolution recurrence stay on only two returned projective lineage classes?
    -> exact counterexample => refute THIRD-LINE theorem
    -> otherwise extract exact third-line forcing identity

A2. TYPE-P -> REGISTERED EVENT/CHANNEL REALIZATION
    Build the typed bridge without identifying projective and scalar-phase holonomy.

A3. IDENTITY-MONODROMY EQUALITY BRANCH
    Classify productive M=I, zero-defect/no-exit webs.

A4. REUSE EXISTING TYPE-PHI PACKING / FRESH-DONOR DYNAMICS
    Do not reinvent them; apply only after sound realization/coverage.

A5. CLOSE OCSR / Witness Soundness / recursive cancellation
    Convert local exact identities into G6-compatible recurrent exclusion.

A6. G4 + G7
    Compactness + exact-zero exclusion -> quantitative gap -> decay recurrence.
```

Theorem statuses after this map:

```text
Three-line projective rigidity                          exact finite PASS / PROPOSAL
Type-P nonidentity third-line return                    OPEN
Type-P identity-monodromy rigidity                      OPEN
Type-P -> Type-Phi channel-realization/coverage         OPEN
Type-Phi packing/window tax                             DERIVED
fresh-donor selected-tree escape                        DERIVED
fresh-donor exact invariance                            REFUTED
fresh-donor response/cancellation window charge         DERIVED
recursive cancellation / recent-ancestry closure        OPEN
OCSR                                                     OPEN
Witness Soundness                                        OPEN
G4                                                       OPEN
G6                                                       OPEN
G7                                                       OPEN
Clay Navier-Stokes regularity                            OPEN
```

---

## 9. Anti-shortcut guard

Do not use any of the following as a substitute for the open bridge:

```text
projective holonomy == scalar phase holonomy
C_k > 0 == physical defect
energy accounting == strict contraction
more fixed-N computation == continuum theorem
cycle-free == weak transfer
cancellation == automatic cost
existential scale contraction == constructive theorem
future-good behavior encoded into the quotient
```

Any new theorem must be registered as `DERIVED`, `OPEN`, `REFUTED`, or `UNRESOLVED`, with exact dependencies and falsifiers.
