# NS P2 — Current Attack Map (2026-09-12)

**Status:** live research map; not a proof claim  
**Canonical architecture:** `paper/NS_P2_STANDALONE_VNEXT.md`  
**Current working PR:** #58 `research/p2-a1-two-line-escape`  
**Claim boundary:** G4 / G6 / G7 / Clay regularity remain OPEN  
**Rule:** Toledo lookup -> Genesis compatibility -> reuse existing object -> derive only missing piece -> mark PROPOSAL.

---

## 0. End-to-end route

```text
finite registered NSE events / readers
        |
        +-----------------------------+
        |                             |
        v                             v
G4 compact scale-covariant      G6 structural zero-defect recurrence rigidity
quotient / ancestry             CR_0(Qbar^sc_P2) subset P union D
OPEN                            OPEN
        |                             |
        +--------------+--------------+
                       v
G7 quantitative exit / positive defect gap                 OPEN
                       |
                       v
V_{j+m} <= (1-delta_*) V_j + epsilon_j
                       |
                       v
S_j -> 0,  R_j = S_j^{r_p} -> 0
                       |
                       v
NS-P2-FINAL-EQUIV(B)                                    DERIVED adapter step
                       |
                       v
regularity under the declared final adapter
```

Nothing here promotes Clay Navier-Stokes regularity beyond **OPEN**.

---

## 1. What is already reusable

The current route reuses rather than rebuilds:

- critical readout identity `R_N = S_N^{r_p}`;
- exact cancellation ledger;
- full local convolution semantics;
- productive-triad exact self-closure;
- general anchor-cross nullity;
- four-anchor no-silent-recruitment / shell-rank degeneracy reductions;
- rank-2 polarization-plane rigidity;
- plane-switch capacity and normal/lineage reconstruction;
- Type-P three-line projective rigidity;
- shared-mode plane-turn trichotomy;
- existing event-level frame-mismatch / Type-Phi phase-tax machinery;
- planar / 2D3C branch;
- fail-closed reader / lineage / defect governance.

Key reusable nullity law:

\[
\ker L_{p,q,a}\neq\{0\}
\iff
(a\cdot q=0)\ \vee\ (|q|=|p|).
\]

Therefore repeated suppression is not a free operation: it must accumulate declared orthogonality, equal-shell, cancellation, exit, terminal or defect conditions.

---

## 2. Type boundary that must remain enforced

Two holonomy objects are distinct.

### Type P — projective polarization / lineage transport

\[
T_e:\mathbb P(k_1^\perp)\to\mathbb P(k_2^\perp),
\qquad T_{\rm loop}(\lambda)=\lambda.
\]

### Type Phi — complex scalar phase-incidence holonomy

This is the object used by the existing finite phase-frustration / packing theorems.

Forbidden shortcut:

```text
nontrivial Type-P holonomy
  -> Type-Phi pi-cycle
  -> packed tax
```

without an actual registered Fourier-event/channel realization.

---

## 3. Current Type-P A1 map — nonidentity monodromy

Let

\[
M=[A]\in PGL(2,\mathbb R),\qquad M\neq I.
\]

For

\[
A=\begin{pmatrix}a&b\\c&d\end{pmatrix},
\]

fixed projective lines satisfy

\[
cX^2+(d-a)XY-bY^2=0,
\]

with discriminant

\[
\Delta_P=(\operatorname{tr}A)^2-4\det A.
\]

Exact finite classification:

```text
Delta_P < 0 -> 0 real fixed projective lines
Delta_P = 0 -> 1 real fixed projective line
Delta_P > 0 -> 2 real fixed projective lines
```

Hence a nondegenerate rank-2 recurrent returned plane can survive the nonidentity branch only on

\[
\boxed{\Delta_P>0}
\]

and then all recurrent returned lineage classes lie on a strict two-line skeleton

\[
\operatorname{RetReturn}(M)\subseteq\{[\ell_+],[\ell_-]\}.
\]

Proposal status: `PROP-P3-TYPEP-FIXEDLINE-CLASSIFICATION-01`.

---

## 4. Two-line skeleton reduction

For an active shared-mode transition, reuse the existing trichotomy:

```text
same plane
OR orthogonal turn
OR positive projective frame dispersion.
```

### Same-plane branch

Routes to the planar / 2D3C regular branch.

### Positive-dispersion branch

Because this is an actual registered Fourier-event geometry, it may enter the existing event-level frame-mismatch / Type-Phi frustration-or-amplitude-cut machinery without type drift.

### Orthogonal-turn branch

Exact full-closure geometry now gives:

```text
nonplanar orthogonal zero-dispersion patch
  -> positive projective frame dispersion within <= 2 geometric closure generations
  OR a required generated channel is suppressed.
```

Proposal: `PROP-P3-ORTHOGONAL-TURN-FULLCLOSURE-ESCAPE-01`.

Therefore the preferred A1 reduction is

\[
\boxed{
M\neq I
\Longrightarrow
\text{planar/2D3C}
\ \vee\
\text{event-level tax/cut}
\ \vee\
\text{forced-descendant suppression}.
}
\]

Proposal/reduction: `PROP-P3-A1-TWO-LINE-ESCAPE-REDUCTION-01`.

A global “third returned lineage” theorem remains a valid stronger certificate but is no longer the required immediate route.

---

## 5. YOU ARE HERE — recursive suppression charging

The current load-bearing question is now:

\[
\boxed{
\text{Can a compact productive zero-defect web repeatedly suppress every forced descendant?}
}
\]

Each suppression must route through the finite alphabet

\[
\boxed{
\text{ORTHOGONALITY}
\ \vee\
\text{EQUAL-SHELL NULLITY}
\ \vee\
\text{EXACT MULTISOURCE CANCELLATION}
\ \vee\
\text{TERMINAL / EXIT / DEFECT}.
}
\]

The desired OCSR-level conclusion is

\[
\boxed{
\text{repeated suppression}
\Longrightarrow
\text{planar}
\ \vee\
\text{rank degeneration}
\ \vee\
\text{scale escape}
\ \vee\
\text{registered reader consequence}.
}
\]

This is still **OPEN**.

This is the current preferred attack vector because it folds A1 back into the canonical Standalone frontier instead of creating another independent global theorem.

---

## 6. Parallel hard branch — A3 identity monodromy

The identity branch

\[
M=I
\]

cannot be killed by fixed-line counting because every projective line is fixed.

Target:

\[
\boxed{
M=I
+\text{productivity}
+\text{zero registered defect/exit}
\Longrightarrow
\text{planar / degenerate / terminal}
}
\]

Status: **OPEN**.

This branch must be attacked independently; it is not a corollary of A1.

---

## 7. Canonical convergence to G6

The current structural route is

```text
A1 nonidentity Type-P
  -> two-line skeleton
  -> planar OR event-level tax/cut OR recursive suppression

A3 identity Type-P
  -> productive identity-monodromy rigidity                     OPEN

recursive suppression
  -> OCSR + global cancellation compatibility + Witness Soundness OPEN

all Type-P / event/channel branches
  -> FNW / interior recurrent rigidity                            OPEN
  -> G6                                                            OPEN
```

The phase-packing / fresh-donor machinery is reused only after actual event/channel realization. It is not the organizing principle of the whole proof.

---

## 8. Independent G4 lane

G4 remains a separate hard gate:

```text
exact scale-covariant dynamically sufficient quotient
+ coherent normalized ancestry
+ compactness / closed admissible path space
```

Status: **OPEN**.

Local/G6 progress does not by itself prove G4.

G4 is needed to convert qualitative exclusion into a uniform quantitative gap at G7.

---

## 9. G7 and downstream route

Once G4 compactness and G6 zero-defect exclusion are both available, the intended quantitative step is

\[
\delta_*=\min_{K_{\rm live}}D_{\rm tot}>0,
\]

leading to

\[
\mathcal V_{j+m}
\le
(1-\delta_*)\mathcal V_j+\varepsilon_j.
\]

Then

\[
S_j\to0,
\qquad
R_j=S_j^{r_p}\to0.
\]

The final finite-observation adapter from `R_j -> 0` to regularity is already audited/derived under its declared assumptions. The missing mathematics is upstream at G4/G6/G7.

---

## 10. Current priority order

```text
CURRENT-1  recursive suppression charging on the A1 two-line skeleton
CURRENT-2  A3 identity-monodromy exact fixture / rigidity attack
CURRENT-3  actual event/channel realization and Witness Soundness
CURRENT-4  OCSR -> FNW / G6
PARALLEL   G4 compact quotient / ancestry lane
THEN       G7 quantitative gap
THEN       R_j -> 0 -> final adapter
```

Do not reopen generic local-triad lemma generation unless an exact falsifier from one of the current branches forces it.
